# ngdevkit - MSYS2 packages

This repository contains the PKGBUILD files necessary to produce
ngdevkit binary packages.

This is still work-in-progress, currently meant to be consumed by
ngdevkit's CI scripts.
